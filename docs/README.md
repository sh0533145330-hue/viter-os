# docs

Project-level documentation lives at `../../docs/` (one level up from `repo/`). This directory is reserved for code-adjacent docs (architecture notes, package READMEs aggregated, generated API docs).
