{
  "name": "@vita/auth",
  "version": "0.0.0",
  "type": "module",
  "license": "UNLICENSED",
  "description": "Auth surface: session, roles, identity providers, audit shape.",
  "private": true,
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "lint": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/ssr": "^0.5.1",
    "@supabase/supabase-js": "^2.45.4",
    "@vita/db": "workspace:*",
    "drizzle-orm": "^0.36.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
