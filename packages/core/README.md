# @vita/core

OSS — MIT. Core SDK primitives: `defineBlock`, wire types, and the runtime contracts every workflow block and connector implements. Imported by `@vita/agents`, `@vita/blocks-builtin`, and downstream apps.
